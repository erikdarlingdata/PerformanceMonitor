<Window x:Class="PerformanceMonitor.Darling.Viewer.AddServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add SQL Server"
        SizeToContent="Height" Width="450"
        MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}"
        MinWidth="450" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        SizeChanged="Dialog_SizeChanged"
        SourceInitialized="Dialog_SourceInitialized"
        LocationChanged="Dialog_LocationChanged"
        Icon="EDD.ico"
        Background="{DynamicResource BackgroundBrush}">
    <!-- #1828: the auth panels grow this dialog past a 1080p work area, and SizeToContent kept growing
         the window off-screen with the buttons unreachable (NoResize left no recovery). The form scrolls
         in a star row, the status + buttons are pinned OUTSIDE the scroll so they are always visible,
         and MaxHeight clamps the window to the work area - the Dashboard twin's proven shape. Mirrors
         Lite's AddServerDialog. -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" x:Name="HeaderText" Text="Add SQL Server Connection" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"
                   Foreground="{DynamicResource ForegroundBrush}"/>

        <!-- Database Engine (#3499). SQL Server stays the checked default so an untouched dialog renders
             exactly the pre-engine-selector form — the regression pin the tests hold. Selecting PostgreSQL
             swaps the engine-specific parts of the form (EngineMode_Changed): the PostgreSQL options panel
             appears, the SQL-Server-only auth modes and connection options hide, and SQL auth is forced,
             because it is the one mode the service's PostgreSQL connect path honors. -->
        <TextBlock Grid.Row="1" Text="Database Engine" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton x:Name="SqlServerEngineRadio" Content="SQL Ser_ver" GroupName="EngineGroup"
                         Foreground="{DynamicResource ForegroundBrush}" IsChecked="True"
                         Checked="EngineMode_Changed" Margin="0,0,16,0"/>
            <RadioButton x:Name="PostgresEngineRadio" Content="Postgre_SQL" GroupName="EngineGroup"
                         Foreground="{DynamicResource ForegroundBrush}"
                         Checked="EngineMode_Changed"
                         ToolTip="PostgreSQL or Amazon Aurora PostgreSQL. Connects with username/password auth; Aurora typically also needs 'Trust server certificate' (it presents an RDS CA a stock trust store does not know)."/>
        </StackPanel>

        <!-- Server Name -->
        <TextBlock Grid.Row="3" Text="Server Name / Address" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="4" x:Name="ServerNameBox" Margin="0,0,0,12"/>

        <!-- Display Name -->
        <TextBlock Grid.Row="5" Text="Display Name (optional)" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="6" x:Name="DisplayNameBox" Margin="0,0,0,12"/>

        <!-- PostgreSQL options (#3499) — shown only on the PostgreSQL arm, so the SQL Server form is
             untouched. Port is the one PostgreSQL-only field; blank means the driver default (5432), the
             same omitted-means-default contract as the MCP add_servers tool's port parameter, so a
             default-port target added here derives the SAME identity as one onboarded over MCP with port
             omitted. TLS reuses the shared Encryption + Trust controls below — the note says how the
             service maps them onto sslmode, so the operator is not left guessing at SQL Server vocabulary. -->
        <StackPanel Grid.Row="7" x:Name="PostgresOptionsPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <TextBlock Text="Port:" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Width="110"/>
                <TextBox x:Name="PortBox" Width="100" TextAlignment="Right"
                         ToolTip="Leave blank for the default (5432). Set it only for a PostgreSQL instance on a non-default port — the port is part of the server's identity, so two instances on one host stay distinct."/>
                <TextBlock Text="blank = 5432, the default" Foreground="{DynamicResource ForegroundMutedBrush}"
                           FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11"
                       Text="PostgreSQL targets connect with username/password authentication. The Database box below is optional — blank connects to the maintenance database ('postgres'), which the cluster-wide collectors read from. Encryption maps to sslmode: Optional → prefer; Mandatory/Strict → verify-full, or require when 'Trust server certificate' is checked (typically needed for Aurora)."/>
        </StackPanel>

        <!-- Authentication. The credential source is either inline per-server auth or a shared credential
             profile (the Darling service resolves the profile at connect time — the viewer never connects).
             This is declarative config the service consumes. -->
        <TextBlock Grid.Row="8" Text="Authentication" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="9" Margin="0,0,0,12">
            <!-- Credential source: inline (per-server) vs a shared credential profile -->
            <RadioButton Content="Configure c_redentials on this server"
                         Foreground="{DynamicResource ForegroundBrush}" IsChecked="True"
                         Checked="CredentialSource_Changed" Margin="0,0,0,4"/>
            <RadioButton x:Name="UseProfileRadio" Content="_Use a credential profile"
                         Foreground="{DynamicResource ForegroundBrush}"
                         Checked="CredentialSource_Changed" Margin="0,0,0,4"
                         ToolTip="Use a named, shared credential (set up under Manage Servers → Credential Profiles). The profile fully overrides this server's auth type and credentials."/>

            <!-- Profile picker (shown when 'Use a credential profile' is selected) -->
            <StackPanel x:Name="ProfilePickerPanel" Visibility="Collapsed" Margin="0,4,0,0">
                <TextBlock Text="Credential Profile" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
                <ComboBox x:Name="ProfileComboBox" DisplayMemberPath="PickerDisplay"/>
                <TextBlock x:Name="NoProfilesNote" TextWrapping="Wrap" FontSize="11" Visibility="Collapsed"
                           Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,4,0,0"
                           Text="No credential profiles exist yet. Create one under Manage Servers → Credential Profiles."/>
            </StackPanel>

            <StackPanel x:Name="InlineAuthRadios" Margin="0,4,0,0">
                <RadioButton x:Name="WindowsAuthRadio" Content="_Windows Authentication" Foreground="{DynamicResource ForegroundBrush}"
                             IsChecked="True" Checked="AuthMode_Changed" Margin="0,0,0,4"/>
                <RadioButton x:Name="SqlAuthRadio" Content="S_QL Server Authentication" Foreground="{DynamicResource ForegroundBrush}"
                             Checked="AuthMode_Changed" Margin="0,0,0,4"/>
                <RadioButton x:Name="EntraMfaAuthRadio" Content="Microsoft _Entra MFA" Foreground="{DynamicResource ForegroundBrush}"
                             Checked="AuthMode_Changed"
                             ToolTip="Interactive authentication with MFA for Azure SQL Database."/>
                <RadioButton x:Name="ServicePrincipalAuthRadio" Content="Azure — Service _Principal" Foreground="{DynamicResource ForegroundBrush}"
                             Checked="AuthMode_Changed" Margin="0,4,0,0"
                             ToolTip="Non-interactive Azure SQL auth using an Entra app registration (client id + secret)."/>
                <RadioButton x:Name="ManagedIdentityAuthRadio" Content="Azure — _Managed Identity" Foreground="{DynamicResource ForegroundBrush}"
                             Checked="AuthMode_Changed" Margin="0,4,0,0"
                             ToolTip="Non-interactive Azure SQL auth using the managed identity of the Azure resource running the Darling service."/>
            </StackPanel>
        </StackPanel>

        <!-- SQL Auth Credentials -->
        <StackPanel Grid.Row="10" x:Name="SqlCredentialsPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <TextBlock Text="Username" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Margin="0,0,0,8"/>
            <TextBlock Text="Password" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox"/>
        </StackPanel>

        <!-- Entra MFA Credentials -->
        <StackPanel Grid.Row="10" x:Name="EntraMfaPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <TextBlock Text="Username (optional)" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="EntraMfaUsernameBox" Margin="0,0,0,8"
                     ToolTip="Optional: the UPN the service should pre-populate the authentication dialog with."/>
        </StackPanel>

        <!-- Service Principal Credentials -->
        <StackPanel Grid.Row="10" x:Name="ServicePrincipalPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <TextBlock Text="Client (Application) ID" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="AzureClientIdBox" Margin="0,0,0,8"
                     ToolTip="The Application (client) ID of the Entra app registration / service principal."/>
            <TextBlock Text="Client Secret" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <PasswordBox x:Name="AzureClientSecretBox" Margin="0,0,0,8"/>
            <TextBlock Text="Tenant ID (optional)" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="AzureTenantIdBox" Margin="0,0,0,4"
                     ToolTip="Optional. Stored for reference; the tenant is resolved automatically from the target server."/>
        </StackPanel>

        <!-- Managed Identity Credentials -->
        <StackPanel Grid.Row="10" x:Name="ManagedIdentityPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <TextBlock Text="User-Assigned Identity Client ID (optional)" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="ManagedIdentityClientIdBox" Margin="0,0,0,8"
                     ToolTip="Leave blank to use the system-assigned managed identity. Set to a client id to use a specific user-assigned identity."/>
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11"
                       Text="Managed Identity only works when the Darling service runs on an Azure VM / resource that has a managed identity assigned. Use Service Principal for non-interactive auth from a workstation."/>
        </StackPanel>

        <!-- Connection Options -->
        <TextBlock Grid.Row="11" Text="Connection Options" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="12" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Encryption:" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Width="110"/>
                <ComboBox x:Name="EncryptModeComboBox" Width="150" SelectedIndex="1">
                    <ComboBoxItem Content="Optional"/>
                    <ComboBoxItem Content="Mandatory"/>
                    <ComboBoxItem Content="Strict"/>
                </ComboBox>
            </StackPanel>
            <CheckBox x:Name="TrustCertCheckBox" Content="Trust ser_ver certificate (skip certificate validation)"
                      Foreground="{DynamicResource ForegroundBrush}" IsChecked="False"/>
            <CheckBox x:Name="EnabledCheckBox" Content="E_nable data collection for this server"
                      Foreground="{DynamicResource ForegroundBrush}" IsChecked="True" Margin="0,6,0,0"/>
            <CheckBox x:Name="FavoriteCheckBox" Content="Mar_k as favorite (appears at top of list)"
                      Foreground="{DynamicResource ForegroundBrush}" Margin="0,6,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <TextBlock Text="Database:" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Width="110"/>
                <TextBox x:Name="DatabaseNameBox" Width="250"
                         ToolTip="Required for Azure SQL Database. Leave empty for on-premises SQL Server."/>
            </StackPanel>
            <CheckBox x:Name="ReadOnlyIntentCheckBox" Content="Read-_only intent (for AG listeners and readable replicas)"
                      Foreground="{DynamicResource ForegroundBrush}" Margin="0,6,0,0"
                      ToolTip="Sets ApplicationIntent=ReadOnly. Required when connecting via an AG listener or Azure failover group endpoint to route to a readable secondary."/>
            <CheckBox x:Name="MultiSubnetFailoverCheckBox" Content="Multi-subnet fai_lover (for AG listeners and FCIs)"
                      Foreground="{DynamicResource ForegroundBrush}" Margin="0,6,0,0"
                      ToolTip="Sets MultiSubnetFailover=true. Connects to all IPs in parallel during failover. Recommended for AG listeners and failover cluster instances spanning multiple subnets."/>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <TextBlock Text="Monthly Cost ($):" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Width="110"/>
                <TextBox x:Name="MonthlyCostBox" Width="100" TextAlignment="Right" Text="0"
                         ToolTip="What this server costs per month (license, compute, storage combined). Used for FinOps cost attribution. Leave 0 to hide cost columns."/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <TextBlock Text="Alert delivery:" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Width="110"/>
                <ComboBox x:Name="AlertDeliveryOverrideBox" Width="240" SelectedIndex="0"
                          ToolTip="How deadlock/blocking alerts for THIS server are delivered. 'Use global setting' follows the app-wide Alerts setting; override to force Summary or Per-event for just this server.">
                    <ComboBoxItem Content="Use global setting"/>
                    <ComboBoxItem Content="Summary — one card per cycle"/>
                    <ComboBoxItem Content="Per-event — one notification per incident"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

            </Grid>
        </ScrollViewer>

        <!-- Status + buttons: pinned below the scroll so they stay visible at any window height.
             Test Connection enqueues a test_connect command; the SERVICE opens the connection and
             writes the probe result back, which the dialog polls for and shows. -->
        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource ForegroundMutedBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,8"/>
            <DockPanel LastChildFill="False">
                <Button x:Name="TestConnectionButton" Content="_Test Connection" Width="130" Height="30"
                        Click="TestConnectionButton_Click" DockPanel.Dock="Left"
                        ToolTip="Asks the Darling service to connect to this server and report the version/edition, before saving."/>
                <Button Content="_Cancel" Width="80" Height="30" Click="CancelButton_Click" DockPanel.Dock="Right" IsCancel="True"/>
                <Button x:Name="SaveButton" Content="_Save" Width="80" Height="30" Click="SaveButton_Click" Style="{StaticResource AccentButton}" DockPanel.Dock="Right" Margin="0,0,8,0"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
