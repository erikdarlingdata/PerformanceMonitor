<Window x:Class="PerformanceMonitor.Darling.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PerformanceMonitor.Darling.Viewer"
        xmlns:ui="clr-namespace:PerformanceMonitor.Ui;assembly=PerformanceMonitor.Ui"
        Title="Performance Monitor Darling"
        Width="1280"
        Height="800"
        MinWidth="900"
        MinHeight="600"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Icon="EDD.ico"
        Background="{DynamicResource BackgroundLightBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <!--
        Theme-aware shell (headless plan). The window's chrome colors now resolve from the shared
        theme dictionary via {DynamicResource} (merged app-wide in App.xaml) — the same keys the
        copied Lite XAML (the per-server ViewerServerTab and the later tab ports) already use — so
        this window follows the active Dark/Light/CoolBreeze theme. Each token's Dark value equals
        the hex it replaced (#22252b chrome, #111217 wells, #2a2d35 borders, #E4E6EB text), so Dark
        renders exactly as before; only a few semantic/keep colors (offline grey, badge text, modal
        scrim, favorite-star gold) stay inline.

        Navigation mirrors Lite's shape (headless-plan W0 IA inversion): a left sidebar server list,
        and ONE top tab strip holding the fixed aggregate tabs (Overview, Alert History, FinOps,
        Recommendations — matching Lite's order — plus the hidden-until-used Plan Viewer) and the
        dynamically-added closable per-server tabs. Overview and Alert History are aggregates over ALL
        servers (Overview is the server-cards grid; Alert History is the all-servers history, filterable
        by server); FinOps and Recommendations are server-scoped, their own pickers synced to the sidebar
        selection. Single-click a server drives the server-scoped aggregate tabs to it; double-click a
        server row (or an Overview card) opens (or focuses) that server's ViewerServerTab. The fleet-refresh
        timer (the configurable NocRefreshIntervalSeconds) refreshes the visible tab (an aggregate tab or the
        visible server tab's active inner tab); the Overview has its own timer at the same interval.
    -->
    <Window.Resources>
        <local:StatusToBrushConverter x:Key="StatusToBrush"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:FleetDepthToIndentConverter x:Key="FleetDepthToIndent"/>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="EmptyHint" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundMutedBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="MaxWidth" Value="480"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <!-- One dark grid chrome shared by the aggregate-tab data grids (hoisted from the wave-1
             HealthGrid inline styles — same setters, zero visual change). -->
        <Style x:Key="DarkGridHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource ViewerGridHeaderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="DarkGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkGridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ViewerSelectionBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkGrid" TargetType="DataGrid">
            <!-- DarkGrid has no BasedOn, so it must re-declare the implicit DataGrid style's behaviors or grids using it silently lose them (sort-descending-first, header-copy, middle-click panning). -->
            <Setter Property="ui:DataGridClipboardBehavior.FixHeaderCopy" Value="True"/>
            <Setter Property="ui:DataGridSortBehavior.SortDescendingFirst" Value="True"/>
            <Setter Property="ui:ScrollPanBehavior.EnableMiddleClickPanning" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundDarkBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{DynamicResource BackgroundDarkBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource ViewerAltRowBrush}"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource DarkGridHeader}"/>
            <Setter Property="RowStyle" Value="{StaticResource DarkGridRow}"/>
            <Setter Property="CellStyle" Value="{StaticResource DarkGridCell}"/>
        </Style>

        <Style x:Key="DarkTabItem" TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                CornerRadius="3,3,0,0"
                                Padding="14,6"
                                Margin="0,0,2,0">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerSelectionBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox / Button used by the Recommendations tab's own server selector + Refresh
             button (they match the hardcoded Dark palette instead of the default light WPF chrome). -->
        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerSelectionBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ViewerSelectionBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ViewerButtonBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerButtonBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerDisabledBgBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ViewerDisabledTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundDarkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{DynamicResource BackgroundDarkBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource ForegroundMutedBrush}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{DynamicResource ViewerAltRowBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}" MaxHeight="240">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="280" MinWidth="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: the server list. Single-click drives the aggregate tabs; double-click opens
                 (or focuses) that server's per-server tab. The header's « / » button collapses the
                 sidebar to a thin rail (mirrors Lite's ToggleSidebar). -->
            <DockPanel Grid.Column="0" Margin="0,0,10,0">
                <Grid DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" x:Name="SidebarHeaderText">
                        <TextBlock Text="Servers" Style="{StaticResource SectionHeader}" Margin="0,0,0,2"/>
                        <TextBlock Text="Double-click to open a server tab" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" Margin="0,0,0,6"/>
                    </StackPanel>
                    <Button Grid.Column="1" x:Name="SidebarToggleButton" Content="«" Click="ToggleSidebar_Click"
                            Style="{StaticResource DarkButton}"
                            ToolTip="Collapse/expand the sidebar" VerticalAlignment="Top"
                            Width="24" Height="24" Padding="0" FontSize="14"/>
                </Grid>

                <!-- Server search: filters the list/tree below by name or tag, live. Slots into
                     FleetView.Rebuild (the reserved seam) so grouping and selection follow automatically.
                     A hit-test-invisible watermark shows the placeholder while the box is empty. -->
                <Grid x:Name="ServerSearchRow" DockPanel.Dock="Top" Margin="0,0,0,8">
                    <TextBox x:Name="ServerSearchBox"
                             TextChanged="ServerSearchBox_TextChanged"
                             ToolTip="Filter the list by server name or tag"/>
                    <TextBlock Text="Search servers or tags…"
                               IsHitTestVisible="False"
                               Foreground="{DynamicResource ForegroundMutedBrush}"
                               VerticalAlignment="Center" HorizontalAlignment="Left"
                               Margin="8,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=ServerSearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Sidebar footer: the button stack pinned to the bottom of the server list, ported to
                     parity with Lite. Server management (Add Server / Manage Servers) writes the viewer's
                     own registry; the service honoring it is separate wiring (see the PR). Import Data
                     (Lite's DuckDB import) is omitted — it has no viewer meaning. Declared as the first
                     Bottom-docked child so it sits at the very bottom, below the no-servers hint. -->
                <Border x:Name="SidebarFooter" DockPanel.Dock="Bottom"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                        Margin="0,8,0,0" Padding="0,8,0,0">
                    <StackPanel>
                        <Button Click="AddServerButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                ToolTip="Add a monitored SQL Server to the viewer's registry">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Add Server" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="AddMultipleServersButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Paste a list of servers and add them all at once with shared credentials">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Add Multiple" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ManageServersButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Add, edit, or remove monitored servers">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE8FD;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Manage Servers" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <!-- #2595: tag management had NO discoverable entry point here. Its only door was
                             the group-header context menu, which means right-clicking a row labelled
                             "Untagged" — and that row only renders when at least one server is untagged, so
                             on a viewer with no servers yet tag management was unreachable entirely. Lite has
                             carried a visible Manage Tags button in this same footer all along, so this is a
                             parity gap rather than a new surface. ManageTags_Click already carries its own
                             read-only backstop (#2008), so the button needs no gating of its own. -->
                        <Button Click="ManageTags_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Create, rename, nest, and assign tags across the fleet">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE8EC;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Manage Tags" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="OpenPlanViewerButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Open or paste an execution plan (independent of any server)">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Open Plan Viewer" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ImportSettingsButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Import server definitions and settings from a previous viewer install">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE8FA;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Import Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ViewLogButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Open the current application log file">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="View Log" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="OpenLogFolderButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Open the log folder in Explorer (for grabbing logs to attach to bug reports)">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Open Log Folder" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="SettingsButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="Viewer settings — defaults for newly-opened server tabs">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Click="AboutButton_Click"
                                Style="{StaticResource DarkButton}"
                                Padding="8,5"
                                Margin="0,8,0,0"
                                ToolTip="About Performance Monitor Darling">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE897;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="About" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="ServersHintText"
                           DockPanel.Dock="Bottom"
                           Text="no servers registered — is the Darling service running?"
                           Foreground="{DynamicResource ForegroundMutedBrush}"
                           TextWrapping="Wrap"
                           Margin="2,8,2,0"
                           Visibility="Collapsed"/>
                <ListBox x:Name="ServerList"
                         Background="{DynamicResource BackgroundDarkBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1"
                         Foreground="{DynamicResource ForegroundBrush}"
                         SelectionChanged="ServerList_SelectionChanged"
                         MouseDoubleClick="ServerList_MouseDoubleClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}"
                                                CornerRadius="3"
                                                Margin="2">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ViewerSelectionBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.Resources>
                        <DataTemplate x:Key="FleetServerRowTemplate">
                            <!-- Ported from Lite's server-row template: a freshness status dot (green/amber/red
                                 from DotStatus), the name + version, a favorite star, and the right-click menu.
                                 Connect/Disconnect are omitted — the viewer never connects to monitored servers
                                 (it reads only the Postgres store). Transparent Border hosts the context menu so
                                 the whole row is a right-click target; the item background comes from the
                                 container style above. -->
                            <Border Background="Transparent"
                                    Margin="{Binding Depth, Converter={StaticResource FleetDepthToIndent}}"
                                    ContextMenuOpening="ServerRow_ContextMenuOpening">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="_Toggle Favorite" Click="ServerContextMenu_ToggleFavorite_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x2B50;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <!-- Populated on open by ServerRow_ContextMenuOpening, which locates this
                                             item by Tag rather than by Header text (the header changes when it
                                             gains a mnemonic; the tag does not). -->
                                        <MenuItem Header="_Assign Tags" Tag="AssignTags">
                                            <MenuItem.Icon><TextBlock Text="&#x1F3F7;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="_Edit..." Click="ServerContextMenu_Edit_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x270F;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="_Remove" Click="ServerContextMenu_Remove_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x1F5D1;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <!-- One-click whole-server alert silence: writes/removes a server-scoped
                                             mute rule the running service honors (email/Teams/Slack + in-app toasts),
                                             the shortcut over the multi-step Manage Mute Rules dialog. -->
                                        <!-- Mutually exclusive since #2031 (Lite's semantics): ServerRow_ContextMenuOpening
                                             DISABLES the inapplicable one (never hides it) from the polled silenced state,
                                             located by Tag like the Assign Tags item above. -->
                                        <MenuItem Header="_Silence This Server" Tag="SilenceServer" Click="ServerContextMenu_Silence_Click"
                                                  ToolTip="Mute every alert for this server until you Unsilence it">
                                            <MenuItem.Icon><TextBlock Text="&#x1F507;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="_Unsilence" Tag="UnsilenceServer" Click="ServerContextMenu_Unsilence_Click"
                                                  ToolTip="Remove this server's whole-server alert silence">
                                            <MenuItem.Icon><TextBlock Text="&#x1F514;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <!-- Acknowledge: quiet the alert badge (viewer-local) until a NEW alert
                                             arrives — unlike Silence, it does NOT stop the service's delivery. -->
                                        <MenuItem Header="Ac_knowledge Alerts" Click="ServerContextMenu_Acknowledge_Click"
                                                  IsEnabled="{Binding Server.HasAttention}"
                                                  ToolTip="Quiet this server's alert badge until a new alert arrives (delivery continues)">
                                            <MenuItem.Icon><TextBlock Text="&#x2713;" FontWeight="Bold"/></MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Status dot (collection freshness) + the muted-bell silence indicator
                                         (#2031: a silenced server must not look healthy-quiet).

                                         #2473: the dot renders the same ServerCollectionStatus the Overview
                                         card does, and carries the same first line the card's tooltip opens
                                         on. It had triggers for three of the five states, so a registered-
                                         but-never-collected server fell through to the muted default and
                                         went grey "Unknown" while the card said amber "Awaiting first
                                         collection" about that same server, one panel over.

                                         There is deliberately still NO trigger for "Unknown": grey is the
                                         honest paint for a server whose freshness was never classified, and
                                         it is the muted default precisely so it needs no arm. Every OTHER
                                         member of the enum must have one, which is asserted rather than
                                         commented (ViewerSidebarDotRendersTheCardStatusTests) — a missing
                                         trigger paints a wrong colour and fails nothing, which is exactly
                                         how this defect survived. -->
                                    <StackPanel Grid.Column="0" Grid.RowSpan="2"
                                                Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse Width="10" Height="10"
                                                 Margin="0,0,8,0"
                                                 ToolTip="{Binding Server.DotTooltip}"
                                                 VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="{DynamicResource ForegroundMutedBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Server.DotStatus}" Value="Online">
                                                            <Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Server.DotStatus}" Value="Offline">
                                                            <Setter Property="Fill" Value="{DynamicResource ErrorBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Server.DotStatus}" Value="Warning">
                                                            <Setter Property="Fill" Value="{DynamicResource WarningBrush}"/>
                                                        </DataTrigger>
                                                        <!-- Amber, the colour the card already chose for this state and wrote
                                                             down why: a queued server is not a dead one. Grey would have been
                                                             the smaller change and is arguably honest ("no information yet"),
                                                             but it is a SECOND answer to a question the card has already
                                                             answered on the same screen, which is the whole defect. -->
                                                        <DataTrigger Binding="{Binding Server.DotStatus}" Value="Awaiting first collection">
                                                            <Setter Property="Fill" Value="{DynamicResource WarningBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <!-- A greyed vector bell with a slash (never an emoji), shown only while a
                                             whole-server alert silence is active. -->
                                        <Viewbox Width="11" Height="11" Margin="0,0,6,0" VerticalAlignment="Center"
                                                 ToolTip="Alerts silenced for this server — right-click → Unsilence to restore">
                                            <Viewbox.Style>
                                                <Style TargetType="Viewbox">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Server.IsSilenced}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Viewbox.Style>
                                            <Grid Width="24" Height="24">
                                                <Path Fill="{DynamicResource ForegroundMutedBrush}"
                                                      Data="M12,2 C10.9,2 10,2.9 10,4 L10,4.29 C7.12,5.14 5,7.83 5,11 L5,17 L3,19 L3,20 L21,20 L21,19 L19,17 L19,11 C19,7.83 16.88,5.14 14,4.29 L14,4 C14,2.9 13.1,2 12,2 Z M10,21 A2,2 0 0,0 14,21 Z"/>
                                                <Line X1="3" Y1="3" X2="21" Y2="21"
                                                      Stroke="{DynamicResource ForegroundMutedBrush}" StrokeThickness="2.4"/>
                                            </Grid>
                                        </Viewbox>
                                    </StackPanel>

                                    <!-- Server name -->
                                    <TextBlock Grid.Column="1" Grid.Row="0"
                                               Text="{Binding Server.DisplayName}"
                                               FontSize="13" FontWeight="SemiBold"
                                               Foreground="{DynamicResource ForegroundBrush}"/>

                                    <!-- Details (SQL version) -->
                                    <TextBlock Grid.Column="1" Grid.Row="1"
                                               Text="{Binding Server.VersionLabel}"
                                               FontSize="11"
                                               Foreground="{DynamicResource ForegroundMutedBrush}"/>

                                    <!-- Alert "needs attention" badge: count of active (unacknowledged, un-muted)
                                         alerts for this server, hidden when none. Colour follows severity
                                         (Critical→ErrorBrush, Warning→WarningBrush), matching Lite's
                                         deadlock→red / else→orange-red tab badge. Acknowledge via the row's
                                         right-click "Acknowledge Alerts" (or the tab-header badge). -->
                                    <Border Grid.Column="2" Grid.RowSpan="2"
                                            CornerRadius="7" Padding="5,0" MinWidth="16" Height="15"
                                            VerticalAlignment="Center" Margin="4,0,0,0"
                                            ToolTip="{Binding Server.AttentionTooltip}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Setter Property="Background" Value="{DynamicResource WarningBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Server.HasAttention}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Server.AttentionSeverity}" Value="Critical">
                                                        <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Server.AttentionText}"
                                                   Foreground="White" FontSize="9" FontWeight="Bold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Favorite star -->
                                    <TextBlock Grid.Column="3" Grid.RowSpan="2"
                                               Text="★" FontSize="16"
                                               VerticalAlignment="Center"
                                               Foreground="#FFD700"
                                               Margin="4,0,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Server.IsFavorite}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </DataTemplate>

                        <!-- Group header row: a tag, or the Favorites / Untagged pseudo-group. The chevron
                             reflects expand state and is blank when the group has nothing under it; clicking
                             the row toggles the group (handled in ServerList_SelectionChanged). The count is
                             the group's directly-shown servers, hidden when zero (a container-only tag). -->
                        <DataTemplate x:Key="FleetHeaderRowTemplate">
                            <Border Background="Transparent" Padding="0,1"
                                    Margin="{Binding Depth, Converter={StaticResource FleetDepthToIndent}}"
                                    ContextMenuOpening="TagHeader_ContextMenuOpening">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="_New Tag..." Click="TagHeaderContextMenu_NewTag_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x2795;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <!-- Tag="TagOnly" is how TagHeader_ContextMenuOpening finds the items to
                                             disable on the Favorites / Untagged pseudo-groups. It used to match
                                             on the Header text, which silently broke the moment the labels
                                             gained mnemonics. -->
                                        <MenuItem Header="New _Child Tag..." Tag="TagOnly" Click="TagHeaderContextMenu_NewChildTag_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x1F4C1;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="_Rename..." Tag="TagOnly" Click="TagHeaderContextMenu_Rename_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x270F;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="_Delete Tag" Tag="TagOnly" Click="TagHeaderContextMenu_Delete_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x1F5D1;"/></MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="_Manage Tags..." Click="ManageTags_Click">
                                            <MenuItem.Icon><TextBlock Text="&#x1F3F7;"/></MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Width="14" FontSize="9" VerticalAlignment="Center"
                                               Foreground="{DynamicResource ForegroundMutedBrush}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="&#x25B8;"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                        <Setter Property="Text" Value="&#x25BE;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HasChildren}" Value="False">
                                                        <Setter Property="Text" Value=""/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="12"
                                               Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ServerCount, StringFormat='({0})'}"
                                               FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"
                                               Foreground="{DynamicResource ForegroundMutedBrush}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ServerCount}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>

                        <local:FleetRowTemplateSelector x:Key="FleetRowSelector"
                            ServerTemplate="{StaticResource FleetServerRowTemplate}"
                            HeaderTemplate="{StaticResource FleetHeaderRowTemplate}"/>
                    </ListBox.Resources>
                    <ListBox.ItemTemplateSelector>
                        <StaticResource ResourceKey="FleetRowSelector"/>
                    </ListBox.ItemTemplateSelector>
                </ListBox>
            </DockPanel>

            <!-- Right: the top tab strip. Fixed aggregate tabs (Overview, Recommendations, Alerts) plus
                 the dynamically-added closable per-server tabs (appended in code). Only the visible tab
                 loads (Lite's visible-only rule); the fleet-refresh timer re-reads the visible aggregate tab
                 (Overview on its own timer at the same interval), while per-server tabs self-refresh on their own
                 toolbar auto-refresh timer. -->
            <TabControl x:Name="MainTabs"
                        Grid.Column="1"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0"
                        ItemContainerStyle="{StaticResource DarkTabItem}"
                        SelectionChanged="MainTabs_SelectionChanged">

                <!-- Overview: the all-servers server-cards grid (W2a), copied from Lite's Overview tab
                     (Lite/MainWindow.xaml). One 260px card per server; double-click a card opens that
                     server's tab. Status is derived from collection freshness (see ServerSummaryItem),
                     not a live ping — the viewer has no live connection to the monitored servers. -->
                <TabItem x:Name="OverviewTab" Header="Overview">
                    <DockPanel Margin="0,8,0,0">
                        <!-- Fleet-wide NOC roll-up (the signature capability Darling's single central
                             store enables — the Dashboard/Lite each see one server's database and CANNOT
                             rank/total servers against each other). Sits ABOVE the per-server cards, always
                             visible. Three parts: (1) fleet health summary — total servers + counts by the
                             SAME band #1426's cards compute (ServerSummaryItem.OverallMetricSeverity +
                             freshness, reduced in FleetRollup.ClassifyBand); (2) fleet totals — the
                             cross-server blocking / deadlock SQL aggregate (GetFleetTotalsAsync) + servers
                             with collection failures; (3) the worst-first "Needs Attention" ranking, whose
                             rows open that server's tab. DataContext (a FleetRollup) is set in code-behind
                             after LoadOverviewAsync builds it. -->
                        <Border x:Name="FleetRollupContainer" DockPanel.Dock="Top"
                                Background="{DynamicResource BackgroundLightBrush}"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6"
                                Margin="6,0,6,10" Padding="16,12" Visibility="Collapsed">
                            <StackPanel>
                                <!-- Header -->
                                <DockPanel Margin="0,0,0,10">
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- #2424: the needs-attention filter over the card grid below. It sits
                                             beside Sort because it is the same kind of thing — a view control over
                                             the same cards — and it lives in the roll-up header, which is docked and
                                             never scrolls away, so an active filter can never be off screen while the
                                             grid it shrank is on it. The count beside it (set in ApplyOverviewCardFilter)
                                             is what stops a filtered grid reading as an unfiltered one. -->
                                        <CheckBox x:Name="OverviewAttentionOnlyCheck" Content="Needs attention only"
                                                  FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"
                                                  ToolTip="Show only the cards that are not Healthy — Critical, Warning, Offline, or awaiting a first collection. Uncheck to show the whole fleet again."
                                                  Checked="OverviewAttentionOnlyCheck_Changed"
                                                  Unchecked="OverviewAttentionOnlyCheck_Changed"/>
                                        <TextBlock x:Name="OverviewAttentionCountText" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource WarningBrush}" Margin="0,0,14,0"
                                                   Visibility="Collapsed"/>
                                        <TextBlock Text="Sort:" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,6,0"/>
                                        <ComboBox x:Name="OverviewSortSelector" Width="120" VerticalAlignment="Center"
                                                  Style="{StaticResource DarkCombo}"
                                                  SelectionChanged="OverviewSortSelector_SelectionChanged">
                                            <ComboBoxItem Content="CPU %"/>
                                            <ComboBoxItem Content="Name"/>
                                        </ComboBox>
                                    </StackPanel>
                                    <TextBlock Text="Fleet Health" FontSize="16" FontWeight="SemiBold"
                                               Foreground="{DynamicResource ForegroundBrush}"/>
                                    <TextBlock Text="{Binding MonitoringText}" FontSize="11"
                                               Foreground="{DynamicResource ForegroundMutedBrush}"
                                               VerticalAlignment="Center" Margin="12,1,0,0"
                                               TextTrimming="CharacterEllipsis"/>
                                </DockPanel>

                                <!-- (1) Health-band summary tiles: total + counts by band. Tile colours are
                                     the card severity palette (green / amber / red / grey-for-offline). -->
                                <WrapPanel Margin="0,0,0,12">
                                    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}"
                                            BorderThickness="1" CornerRadius="5" MinWidth="86" Margin="0,0,8,0" Padding="12,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding TotalServers}" FontSize="22" FontWeight="Bold"
                                                       Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Servers" FontSize="10" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource SuccessBrush}"
                                            BorderThickness="1" CornerRadius="5" MinWidth="86" Margin="0,0,8,0" Padding="12,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding HealthyCount}" FontSize="22" FontWeight="Bold"
                                                       Foreground="{DynamicResource SuccessBrush}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Healthy" FontSize="10" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource WarningBrush}"
                                            BorderThickness="1" CornerRadius="5" MinWidth="86" Margin="0,0,8,0" Padding="12,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding WarningCount}" FontSize="22" FontWeight="Bold"
                                                       Foreground="{DynamicResource WarningBrush}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Warning" FontSize="10" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource ErrorBrush}"
                                            BorderThickness="1" CornerRadius="5" MinWidth="86" Margin="0,0,8,0" Padding="12,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding CriticalCount}" FontSize="22" FontWeight="Bold"
                                                       Foreground="{DynamicResource ErrorBrush}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Critical" FontSize="10" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="#888888"
                                            BorderThickness="1" CornerRadius="5" MinWidth="86" Margin="0,0,8,0" Padding="12,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding OfflineCount}" FontSize="22" FontWeight="Bold"
                                                       Foreground="#888888" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Offline" FontSize="10" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </WrapPanel>

                                <!-- (2) Fleet totals: the cross-server aggregate over the last hour (the
                                     same window the per-server cards use, so these reconcile with the card
                                     counts). -->
                                <WrapPanel Margin="0,0,0,12">
                                    <TextBlock Text="Last hour:" FontSize="11" FontWeight="SemiBold"
                                               Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,16,0" VerticalAlignment="Center"
                                               Text="{Binding TotalBlockingEvents, StringFormat='Blocking events: {0}'}"/>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,16,0" VerticalAlignment="Center"
                                               Text="{Binding TotalDeadlocks, StringFormat='Deadlocks: {0}'}"/>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,16,0" VerticalAlignment="Center"
                                               Text="{Binding CollectionFailuresText, StringFormat='Collection failures: {0}'}"/>
                                </WrapPanel>

                                <!-- (3) Worst-first "Needs Attention" ranking — problem servers only, opens
                                     the server's tab on click. Hidden and replaced by the all-clear line
                                     when the fleet is healthy (toggled in code-behind). -->
                                <TextBlock Text="Needs Attention" FontSize="12" FontWeight="SemiBold"
                                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
                                <ItemsControl x:Name="FleetWorstServersList" ItemsSource="{Binding WorstServers}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Cursor="Hand" Padding="2,3" Margin="0,1"
                                                    ToolTip="Open this server's tab"
                                                    MouseLeftButtonUp="FleetWorstServer_Click">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <DockPanel LastChildFill="True">
                                                    <Ellipse Width="9" Height="9" Fill="{Binding BandBrush}"
                                                             VerticalAlignment="Center" Margin="0,0,8,0" DockPanel.Dock="Left"/>
                                                    <TextBlock Text="{Binding BandLabel}" Foreground="{Binding BandBrush}"
                                                               FontSize="11" FontWeight="SemiBold" Width="58"
                                                               VerticalAlignment="Center" DockPanel.Dock="Left"/>
                                                    <TextBlock Text="{Binding DisplayName}" Foreground="{DynamicResource ForegroundBrush}"
                                                               FontSize="11" FontWeight="SemiBold" Width="160"
                                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" DockPanel.Dock="Left"/>
                                                    <TextBlock Text="{Binding Reason}" Foreground="{DynamicResource ForegroundMutedBrush}"
                                                               FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                </DockPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <!-- #2424: the overflow line is the way TO the overflow, not a report that it
                                     exists. It was a bare TextBlock: on a 57-server fleet it read "+52 more need
                                     attention" and the reporter's question was literally "where do I find these?".
                                     Clicking it turns on the needs-attention card filter, so the answer is the grid
                                     immediately below, with every metric row those 52 servers have. Underlined and
                                     Cursor=Hand because a line that navigates has to look like one. -->
                                <Border x:Name="FleetAdditionalProblems" HorizontalAlignment="Left"
                                        Cursor="Hand" Padding="2,1" CornerRadius="3"
                                        Margin="15,2,0,0" Visibility="Collapsed"
                                        ToolTip="Show only the servers that need attention"
                                        MouseLeftButtonUp="FleetAdditionalProblems_Click">
                                    <!-- The hover idiom is the Needs Attention row's, a few lines up: a
                                         transparent Border that lights on IsMouseOver. Same gesture, same
                                         feedback, so the overflow line reads as one more thing you can click. -->
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ViewerHoverBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock x:Name="FleetAdditionalProblemsText" Text="{Binding AdditionalProblemText}"
                                               FontSize="11" TextDecorations="Underline"
                                               Foreground="{DynamicResource AccentBrush}"/>
                                </Border>
                                <TextBlock x:Name="FleetAllHealthyText" Text="{Binding AllHealthyText}"
                                           FontSize="12" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,0,2" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="OverviewItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource BackgroundLightBrush}"
                                            BorderBrush="{Binding CardBorderBrush}"
                                            BorderThickness="2" CornerRadius="6"
                                            Margin="6"
                                            Width="300" MinHeight="210"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="OverviewCard_MouseLeftButtonDown">
                                        <Grid>
                                            <StackPanel Margin="16">
                                                <DockPanel Margin="0,0,0,4">
                                                    <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold"
                                                               Foreground="{DynamicResource ForegroundBrush}"/>
                                                    <Ellipse Width="10" Height="10" Fill="{Binding StatusBrush}"
                                                             ToolTip="{Binding StatusTooltip}"
                                                             HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </DockPanel>
                                                <!-- #2422: the status line carries the reason it is that colour. The
                                                     card already computed the sentence (FleetRollup.BuildReason, from
                                                     these very metric rows) and spent it only on the Needs Attention
                                                     ranking. Background=Transparent is load-bearing: a TextBlock with a
                                                     null Background hit-tests on its glyphs alone, so the tooltip would
                                                     appear over the letters and nowhere between them. -->
                                                <TextBlock Text="{Binding StatusDisplay}" Foreground="{Binding StatusBrush}"
                                                           Background="Transparent" ToolTip="{Binding StatusTooltip}"
                                                           FontSize="11" Margin="0,0,0,10"/>
                                                <!-- Tag pills (#2008 2a): each server's tags as coloured chips.
                                                     Collapsed when the server carries no tags so untagged cards
                                                     are unchanged. -->
                                                <ItemsControl ItemsSource="{Binding TagPills}" Margin="0,0,0,10">
                                                    <ItemsControl.Style>
                                                        <Style TargetType="ItemsControl">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding TagPills.Count}" Value="0">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ItemsControl.Style>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{Binding Fill}" CornerRadius="3"
                                                                    Padding="6,1" Margin="0,0,4,4">
                                                                <TextBlock Text="{Binding Name}" FontSize="10"
                                                                           Foreground="{Binding TextBrush}"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <!-- Metric rows: [severity dot] [label] [value] [detail],
                                                     mirroring the Dashboard's ServerHealthCard. Each dot +
                                                     value is coloured by the metric's band (ServerSummaryItem
                                                     reproduces ServerHealthStatus's CASE logic). Threads +
                                                     Collectors are the enrichment rows; Memory / Blocking gain
                                                     resource-semaphore + duration detail. Last Collect keeps
                                                     its neutral footer row (no dot). -->
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- CPU -->
                                                    <Ellipse Grid.Row="0" Grid.Column="0" Width="9" Height="9" Fill="{Binding CpuSeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="CPU:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding CpuDisplay}" FontSize="11" Foreground="{Binding CpuSeverityBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding CpuDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Threads (enrichment) -->
                                                    <Ellipse Grid.Row="1" Grid.Column="0" Width="9" Height="9" Fill="{Binding ThreadsSeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="Threads:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding ThreadsDisplay}" FontSize="11" Foreground="{Binding ThreadsSeverityBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding ThreadsDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Memory (richer: total value + resource-semaphore detail) -->
                                                    <Ellipse Grid.Row="2" Grid.Column="0" Width="9" Height="9" Fill="{Binding MemorySeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Memory:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding MemoryDisplay}" FontSize="11" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding MemoryDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Blocking (richer: count value + max-duration detail) -->
                                                    <Ellipse Grid.Row="3" Grid.Column="0" Width="9" Height="9" Fill="{Binding BlockingSeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="Blocking:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding BlockingDisplay}" FontSize="11" Foreground="{Binding BlockingSeverityBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="3" Grid.Column="3" Text="{Binding BlockingDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Deadlocks -->
                                                    <Ellipse Grid.Row="4" Grid.Column="0" Width="9" Height="9" Fill="{Binding DeadlockSeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="4" Grid.Column="1" Text="Deadlocks:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding DeadlockDisplay}" FontSize="11" Foreground="{Binding DeadlockSeverityBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="4" Grid.Column="3" Text="{Binding DeadlockDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Collectors (enrichment) -->
                                                    <Ellipse Grid.Row="5" Grid.Column="0" Width="9" Height="9" Fill="{Binding CollectorSeverityBrush}" VerticalAlignment="Center" Margin="0,2,6,2"/>
                                                    <TextBlock Grid.Row="5" Grid.Column="1" Text="Collectors:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding CollectorDisplay}" FontSize="11" Foreground="{Binding CollectorSeverityBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="5" Grid.Column="3" Text="{Binding CollectorDetail}" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,2"/>

                                                    <!-- Last Collect (footer row, no severity dot) -->
                                                    <TextBlock Grid.Row="6" Grid.Column="1" Text="Last Collect:" FontSize="11" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,2,8,2"/>
                                                    <TextBlock Grid.Row="6" Grid.Column="2" Text="{Binding LastCollectionDisplay}" FontSize="11" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold" Margin="0,2,8,2"/>
                                                </Grid>
                                            </StackPanel>
                                            <!-- Offline overlay -->
                                            <Border Background="#80000000" CornerRadius="6"
                                                    Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibility}}">
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <TextBlock Text="&#x2717;" FontSize="28" Foreground="{DynamicResource ErrorBrush}"
                                                               HorizontalAlignment="Center"/>
                                                    <TextBlock Text="Offline" FontSize="13" Foreground="{DynamicResource ErrorBrush}"
                                                               FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </ScrollViewer>
                    </DockPanel>
                </TabItem>

                <!-- Alerts: the full all-servers Alert History surface (W2a), a copy of Lite's
                     AlertsHistoryTab hosted as a control. All-servers by default with a Server filter,
                     Dismiss Selected / Dismiss All, column filters, copy/export, and Mute This / Mute
                     Similar + Manage Mute Rules (mute lives here per Erik's faithful-copy decision). It
                     owns its own load, so it isn't scoped by the sidebar selection. Ordered right after
                     Overview to match Lite (Overview -> Alert History -> FinOps -> Recommendations). -->
                <TabItem x:Name="AlertsTab" Header="Alert History">
                    <local:AlertsHistoryTab x:Name="AlertsHistoryContent" Margin="0,8,0,0"/>
                </TabItem>

                <!-- Job History: the fleet-wide retained SQL Agent job-run history (issue #1433), a copy of
                     Lite's JobHistoryTab. All-servers by default with Server / Status / Category filters,
                     per-step rows, failure + long-runtime + retry color-coding, and CSV export. Self-loading
                     (not scoped by the sidebar selection), ordered right after Alert History to match Lite. -->
                <TabItem x:Name="JobHistoryTabItem" Header="Job History">
                    <local:JobHistoryTab x:Name="JobHistoryContent" Margin="0,8,0,0"/>
                </TabItem>

                <!-- Availability Groups (#991): fleet-wide AG topology, one card per (reporting server, AG).
                     Collapsed until a load actually finds AG rows — Always On is opt-in and most fleets run
                     none, so a permanently visible tab that only ever says "nothing here" is noise. The reveal
                     is driven from MainWindow.xaml.cs off the control's HasAvailabilityGroups. -->
                <TabItem x:Name="AvailabilityGroupsTabItem" Header="Availability Groups" Visibility="Collapsed">
                    <local:AvailabilityGroupsTab x:Name="AvailabilityGroupsContent" Margin="0,8,0,0"/>
                </TabItem>

                <!-- FinOps: the top-level cross-server aggregate tab (restored to Lite's shape from the earlier
                     per-server inner tab, which had duplicated the fleet-wide Server Inventory inside every
                     server's tab). Its OWN server selector (synced to the sidebar selection on a single-click,
                     mirroring the Recommendations tab, and independently changeable while the tab is open)
                     drives the eleven per-server sub-tabs; Server Inventory stays cross-server and now lives
                     ONCE here. The control owns its own load (server selector + sub-tab activation + the
                     shell's fleet-refresh timer). "View Plan" on its query grids raises PlanRequested, which the
                     shell routes into the standalone Plan Viewer surface. -->
                <TabItem x:Name="FinOpsTab" Header="FinOps">
                    <local:FinOpsTab x:Name="FinOpsContent" Margin="0,8,0,0"/>
                </TabItem>

                <!-- Recommendations: re-skinned to Lite's advise-only card design (W2b). Its OWN server
                     selector (synced to the sidebar selection on a single-click, independently changeable), a Refresh button, and a status line that
                     shows the last analysis time; then a scrollable list of collapsible Expander
                     sections grouped by incident, each holding severity-banded cards (badge + [db] +
                     title + advice + Open in Active Queries + Copy fix + Ask AI). Advise-only: no Apply, and (per the re-skin
                     brief) no mute — mute lives on the Alert History surface. Card templates copy Lite's
                     RecommendationsTab.xaml and resolve their {DynamicResource} theme keys from the
                     app-merged Dark theme. "Generate now" asks the Darling service to run an immediate
                     analysis pass for the selected server (analyze_now) and then refreshes; the status line
                     also surfaces the last analysis time. A read-only seat can't command the service, so the
                     button is disabled. NOT on the fleet-refresh timer (tab-activation refresh only,
                     matching Lite). -->
                <TabItem x:Name="RecommendationsTab" Header="Recommendations">
                    <TabItem.Resources>
                        <BooleanToVisibilityConverter x:Key="RecoBoolToVis"/>

                        <!-- One card: severity badge + [database] + title + advice, then advise-only
                             affordances (Open in Active Queries for incidents + Copy fix when derivable +
                             Ask AI). Copied from Lite's LiteRecommendationCardTemplate; the Open-in-Active-
                             Queries deep-link is the Dashboard incident affordance ported to the viewer. -->
                        <DataTemplate x:Key="RecoCardTemplate">
                            <Border Background="{DynamicResource BackgroundLightBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <!-- Header row: severity badge + [database] -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <Border x:Name="SeverityBadge" CornerRadius="3" Padding="6,1"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource InfoBrush}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding SeverityGlyph}"
                                                           FontFamily="Segoe MDL2 Assets" FontSize="11"
                                                           Foreground="#1A1A1A" VerticalAlignment="Center"
                                                           Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding SeverityLabel}" FontWeight="Bold"
                                                           FontSize="11" Foreground="#1A1A1A"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Text="{Binding DatabaseBracketed}"
                                                   Visibility="{Binding HasDatabase, Converter={StaticResource RecoBoolToVis}}"
                                                   FontFamily="Consolas" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>

                                    <!-- Title -->
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>

                                    <!-- Advice prose -->
                                    <TextBlock Text="{Binding AdviceText}" TextWrapping="Wrap"
                                               Visibility="{Binding HasAdvice, Converter={StaticResource RecoBoolToVis}}"
                                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>

                                    <!-- Affordances (advise-only): Open in Active Queries (incidents), Copy
                                         fix (when derivable), Ask AI. -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                        <Button Content="Open in Active Queries" Padding="10,3" Margin="0,0,8,0"
                                                Visibility="{Binding ShowOpenInActiveQueries, Converter={StaticResource RecoBoolToVis}}"
                                                Click="OpenInActiveQueries_Click"
                                                ToolTip="Open this server's Active Queries scoped to the finding's time window"/>
                                        <Button Content="Copy fix" Padding="10,3" Margin="0,0,8,0"
                                                Visibility="{Binding ShowCopyFix, Converter={StaticResource RecoBoolToVis}}"
                                                Click="CopyFix_Click"
                                                ToolTip="Copy the suggested T-SQL to the clipboard"/>
                                        <Button Content="Ask AI" Padding="10,3"
                                                Visibility="{Binding ShowAskAi, Converter={StaticResource RecoBoolToVis}}"
                                                Click="AskAi_Click"
                                                ToolTip="Copy an MCP investigation prompt to the clipboard"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <DataTemplate.Triggers>
                                <!-- Severity-driven badge colour, from the theme status brushes. -->
                                <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                    <Setter TargetName="SeverityBadge" Property="Background"
                                            Value="{DynamicResource ErrorBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                    <Setter TargetName="SeverityBadge" Property="Background"
                                            Value="{DynamicResource WarningBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Info">
                                    <Setter TargetName="SeverityBadge" Property="Background"
                                            Value="{DynamicResource InfoBrush}"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>

                        <!-- One incident section: a collapsible Expander whose header names the primary
                             finding + count + severity, and whose body is that incident's card list. -->
                        <DataTemplate x:Key="RecoSectionTemplate">
                            <Expander Header="{Binding Header}" IsExpanded="{Binding IsExpanded, Mode=OneTime}"
                                      Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold"
                                      FontSize="14" Margin="0,0,0,10">
                                <!-- The header is data-derived (finding headline, or the raw fact key such
                                     as SOS_SCHEDULER_YIELD when no advice prose exists), and an Expander's
                                     header presenter parses a single "_" as an access-key marker. Rendering
                                     it through a TextBlock keeps the underscores and claims no Alt key.
                                     Foreground is restated because the themes' implicit TextBlock style
                                     would otherwise win. -->
                                <Expander.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Foreground="{DynamicResource ForegroundBrush}"/>
                                    </DataTemplate>
                                </Expander.HeaderTemplate>
                                <ItemsControl ItemsSource="{Binding Cards}"
                                              ItemTemplate="{StaticResource RecoCardTemplate}"
                                              Margin="0,8,0,0"/>
                            </Expander>
                        </DataTemplate>
                    </TabItem.Resources>

                    <Grid Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header bar: own server selector + Refresh + status (last-analysis time). -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Server:" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,6,0"/>
                            <ComboBox x:Name="RecommendationsServerSelector" Width="260" VerticalAlignment="Center"
                                      Style="{StaticResource DarkCombo}"
                                      SelectionChanged="RecommendationsServerSelector_SelectionChanged">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayName}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <Button Content="Refresh" Margin="10,0,0,0" Padding="12,4"
                                    Click="RecommendationsRefresh_Click"
                                    ToolTip="Re-read recommendations for the selected server"/>
                            <Button x:Name="RecommendationsGenerateButton" Content="Generate now" Margin="8,0,0,0" Padding="12,4"
                                    Style="{StaticResource DarkButton}"
                                    Click="RecommendationsGenerateNow_Click"
                                    ToolTip="Ask the Darling service to run analysis for this server now, then refresh"/>
                            <TextBlock x:Name="RecommendationsStatusText" Margin="12,0,0,0" VerticalAlignment="Center"
                                       FontStyle="Italic" Foreground="{DynamicResource ForegroundMutedBrush}"/>
                        </StackPanel>

                        <!-- Content: exactly one region is visible per state. -->
                        <Grid Grid.Row="1">
                            <ScrollViewer x:Name="RecommendationsScroll" VerticalScrollBarVisibility="Auto"
                                          Padding="10" Visibility="Collapsed">
                                <ItemsControl x:Name="RecommendationsSectionsList"
                                              ItemTemplate="{StaticResource RecoSectionTemplate}"/>
                            </ScrollViewer>

                            <TextBlock x:Name="RecommendationsEmptyText"
                                       Style="{StaticResource EmptyHint}"
                                       Text="All clear — no current recommendations."/>

                            <!-- Insufficient data: the analysis pass has not cleared its 24h data-span
                                 gate yet (the persisted V19 marker). Text is set from the view-model. -->
                            <TextBlock x:Name="RecommendationsInsufficientText"
                                       Style="{StaticResource EmptyHint}"
                                       MaxWidth="520" TextWrapping="Wrap" TextAlignment="Center"
                                       Visibility="Collapsed"/>

                            <TextBlock x:Name="RecommendationsLoadingText"
                                       Style="{StaticResource EmptyHint}"
                                       Text="Loading recommendations…"/>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- Plan Viewer: a standalone, hidden-until-used aggregate tab (mirrors Lite's
                     MainWindowPlanViewerTab) — the one plan surface Lite and the Dashboard both have that
                     the viewer lacked. Server-independent: the user opens or pastes a .sqlplan / plan XML
                     (or drags a file in) and it renders in the SHARED PerformanceMonitor.Ui
                     PlanViewerControl as closable sub-tabs, exactly like the per-server Plan Viewer — but
                     with no store read and no server connection. Opened via the sidebar "Open Plan Viewer"
                     button; the header's close button hides it again. Built in MainWindow.PlanViewer.cs.
                     Its inner sub-tabs pick up the theme's implicit TabItem style automatically (no
                     ItemContainerStyle), matching the per-server PlanTabControl. -->
                <TabItem x:Name="MainWindowPlanViewerTab" Visibility="Collapsed">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Plan Viewer" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <Button Style="{DynamicResource TabCloseButton}"
                                    Click="MainWindowPlanViewerClose_Click"
                                    Margin="4,0,0,0"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid AllowDrop="True" PreviewDrop="MainWindowPlanViewer_Drop"
                          PreviewDragOver="MainWindowPlanViewer_DragOver"
                          Background="Transparent"
                          Focusable="True" KeyDown="MainWindowPlanViewer_KeyDown">
                        <TabControl x:Name="MainWindowPlanTabControl"
                                    Background="Transparent"
                                    BorderThickness="0"/>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- Empty state: no servers registered. Shown in the content column with MainTabs hidden
                 until the store has at least one server (mirrors Lite's no-servers empty state). -->
            <Border x:Name="EmptyStatePanel"
                    Grid.Column="1"
                    Background="Transparent"
                    Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="No Servers Registered"
                               FontSize="24" FontWeight="Light"
                               Foreground="{DynamicResource ForegroundMutedBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Start the Darling service to populate the store, then double-click a server to open its dashboard."
                               FontSize="14" Foreground="{DynamicResource ForegroundMutedBrush}"
                               Margin="0,8,0,0" HorizontalAlignment="Center"
                               TextAlignment="Center" MaxWidth="480" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Full-window message state: missing config / unreachable store. Carries the #1954 diagnostics
             block (which darling.json was read, and the non-secret parse summary) so a field operator can
             tell "wrong file" from "right file, wrong value" without going to find the log. -->
        <Border x:Name="MessageOverlay"
                Grid.Row="0"
                Background="{DynamicResource BackgroundLightBrush}"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="760" Margin="24">
                <TextBlock x:Name="MessageText"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           FontSize="14"
                           Foreground="{DynamicResource ForegroundBrush}"/>

                <StackPanel x:Name="MessageDetailsPanel" Visibility="Collapsed" Margin="0,20,0,0">
                    <TextBlock Text="Configuration this viewer used"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundMutedBrush}"
                               Margin="0,0,0,6"/>
                    <Border Background="{DynamicResource BackgroundDarkBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12,10">
                        <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <!-- A read-only TextBox rather than a TextBlock: WPF text blocks are not
                                 selectable, and the whole point is that the operator can lift these lines
                                 into a bug report. Wrapped rather than side-scrolled, because the longest
                                 lines are the resolved PATHS — the one thing that must be readable without
                                 dragging a scrollbar. -->
                            <TextBox x:Name="MessageDetailsText"
                                     IsReadOnly="True"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="{DynamicResource ForegroundBrush}"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                    <!-- The overlay covers the sidebar, so its View Log button is unreachable exactly when
                         it is most wanted; carry one here. The log holds what this block deliberately
                         omits — the exception and its inner chain. -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <!-- #1954 bullet 3: the layered self-test (DNS → TCP → TLS → auth → schema),
                             run against the SAME connection string the failed connect used; results
                             append to the details block above and go to the log. -->
                        <Button x:Name="RunSelfTestButton"
                                Content="Run self-test"
                                Style="{StaticResource DarkButton}"
                                Click="OnRunSelfTestClick"/>
                        <Button x:Name="CopyDiagnosticsButton"
                                Content="Copy details"
                                Style="{StaticResource DarkButton}"
                                Margin="8,0,0,0"
                                Click="OnCopyDiagnosticsClick"/>
                        <Button Content="View Log"
                                Style="{StaticResource DarkButton}"
                                Margin="8,0,0,0"
                                Click="ViewLogButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Multi-field status bar (ported from Lite, adapted to what the viewer knows): general status,
             which SEAT this viewer holds, collector health across all servers (from collection freshness),
             store database size, overall collection status, and server count.

             SeatText sits first among the fixed fields, immediately right of the message area, because it
             is the only one that describes THIS CONNECTION rather than the fleet, and #2479 exists because
             the answer was previously discoverable only by tripping a refusal. Its text, colour and tooltip
             are all set from ViewerSeatIndicator in the connect flow; the XAML holds only the not-probed-yet
             state, which is what an operator sees if the window opens before OnLoaded finishes. -->
        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="StatusText" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="SeatText" Text="Seat: --" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" x:Name="CollectorHealthText" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="3" x:Name="DatabaseSizeText" Text="Database: --" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="4" x:Name="CollectionStatusText" Text="Collection: --" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="16,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="5" x:Name="ServerCountText" Text="Servers: 0" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
